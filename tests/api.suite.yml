class_name: ApiTester
modules:
    enabled: [PhpBrowser, REST, Asserts]
    config:
        PhpBrowser:
            url: http://localhost/dreamlines/public/api
        REST:
            depends: PhpBrowser
            url: http://localhost/dreamlines/public/api
